import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
  // The service is available acrosse the Application
  // Only 1 intance
})
export class TransferDataService {
  
  constructor() {}
}
